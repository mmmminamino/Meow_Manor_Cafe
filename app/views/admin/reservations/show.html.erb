<!DOCTYPE html>
<html lang="ja">
<html class="fontawesome-i2svg-active fontawesome-i2svg-complete">
    <head></head>
    <main>
        <body>
            <div class="col-xs-10 col-xs-offset-1">
                <div class="row">
                    <div class="col-xs-7">
                    <h2>予約内容詳細</h2>
                    <table class="table table-bordered table-condensed">
                        <tbody>
                            <tr>
                                <th>予約者氏名</th>
                                <td><%= link_to admin_customer_path(@reservation.customer) do %>
                                <%= @reservation.customer.last_name %>
                                <%= @reservation.customer.first_name %>
                                <% end %>
                                </td>
                            </tr>
                            <tr>
                                <th>予約日時</th>
                                <td><%= @reservation.created_at.strftime("%Y-%m-%d %H:%M:%S") %></td>
                            </tr>
                            <tr>
                                <th>訪問人数</th>
                                <td><%= @reservation.number_of_people %>名</td>
                            </tr>
                            <tr>
                                <th>支払方法</th>
                                <td><%= @reservation.payment_methods %></td>
                            </tr>
                            <tr>
                                <th>予約ステータス</th>
                                <td><%= form_with model: @reservation, url: admin_reservation_path(@reservation) do |f| %>
                                <%= f.select :reservation_status, Reservation.reservation_statuses.keys.to_a %>
                                <%= f.submit "更新する", class: "btn btn-success" %>
                                <% end %>
                                </td>
                            </tr>
                        </tbody>
                    </table>
                    </div>
                </div>
            </div>
        </body>
    </main>
</html>
</html>