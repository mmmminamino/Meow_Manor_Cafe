<!DOCTYPE html>
<html lang="ja">
<html class="fontawesome-i2svg-active fontawesome-i2svg-complete">
    <head></head>
    <main>
        <body>
              <div class="row">
                  <div class="col-xs-10 col-xs-offset-1">
                      <h2>予約一覧</h2>
                        <div class="order-table">
                            <table class="table table-hover table-inverse">
                                <thead>
                                    <tr>
                                        <th>予約日時</th>
                                        <th>予約者氏名</th>
                                        <th>訪問日時</th>
                                        <th>支払方法</th>
                                        <th>入金ステータス</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% @reservations.each do |reservation| %>
                                        <tr>
                                            <td><%= link_to reservation.created_at.strftime("%Y-%m-%d %H:%M:%S"), admin_reservation_path(reservation) %></td>
                                        </tr>
                                    <%end%>
                                    <%= paginate @reservations %>
                                </tbody>
                            </table>
                        </div>
                  </div>
              </div>
        </body>
    </main>
</html>