            <div class="row">
                <div class="col-xs-10 col-xs-10 col-xs-offset-1">
                    <h2>カフェメニュー</h2>
                    <table  class="table table-hover table-inverse">
                        <thead>
                            <tr>
                                <th>MENU NAME</th>
                                <th>PRICE</th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                           <% @menu_items.each do |menu_item| %>
                           <tr>
                                <td><%= menu_item.name %>
                                <td><%= menu_item.price_without_tax %>円</td>
                                <td><%= link_to "編集する" ,edit_admin_menu_item_path(menu_item), class: "btn btn_-lg btn-outline-success" %></td>
                            </tr>
                            <%end%>
                        <tbody>
                    </table>
                    <div class="text_right">
                        <%= link_to "新規登録", new_admin_menu_item_path, class:"btn btn_-lg btn-outline-dark" %>
                    </div>
                </div>
            </div>