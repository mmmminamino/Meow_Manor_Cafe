<!DOCTYPE html>
<html lang="ja">
<html class="fontawesome-i2svg-active fontawesome-i2svg-complete">
    <main>
        <body>
            <div class="row">
                <div class="col-xs-10 col-xs-10 col-xs-offset-1">
                    <div class="row">
                        <%= form_with model: @cat, url: admin_cat_path do |f| %>
                        <div class="col-xs-5">
                            <h2>猫の登録情報編集</h2>
                            <div class= "item-new-form">
                                <%= f.file_field :image, accept: "image/*" %>
                            </div>
                          　<div class="product-img" style="margin-top: 20px;">
                                <%= image_tag @cat.image, size: "300x200" %>
                            </div>
                            <div class="col-xs-5 col-xs-offset-2">
                                <table class="table table-hover table-inverse">
                                    <tbody>
                                        <tr>
                                            <th>名前</th>
                                            <td><%= f.text_field :name, class: "form-control" %></td>
                                        </tr>
                                        <tr>
                                            <th>特徴</th>
                                            <td><%= f.text_area :introduction, class: "form-control" %></td>
                                        </tr>
                                        <tr>
                                            <th>年齢</th>
                                            <td><%= f.text_field :age, class: "form-control" %>才</th>
                                        </tr>
                                        <tr>
                                            <th>性別</th>
                                            <td><%= f.radio_button :genders, Cat.genders.key(0) %>
                                            <%= f.label :gender_male, "男の子" %></td>
                                            <td><%= f.radio_button :genders, Cat.genders.key(1) %>
                                            <%= f.label :gender_female, "女の子" %></td>
                                        </tr>
                                        <tr>
                                          <th>種類</th>
                                          <td><%= f.text_field :cat_type %></td>
                                        </tr>
                                    </tbody>
                                </table>
                                <div class="text-right">
                                    <%= f.submit "編集する", class: "btn btn-primary text-right" %>
                                </div>
                            </div>
                        </div>
                        <%end%>
                        <div class="text-right">
                            <%= link_to "削除する", admin_cat_path(@cat), method: :delete, class: "btn btn-danger" %>
                        </div>
                    </div>
                </div>
            </div>
        </body>
    </main>
</html>
</html>